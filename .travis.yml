  language: python
  services:
    - docker
  python:
    - "2.7"
  install:
    - make deps
  script:
    - make test
    - make lint
  after_success:
    - make docker_build
    - make docker_push
deploy:
  provider: heroku
  app: infinite-plains-60004
  api_key: ${HEROKU_API_KEY}
